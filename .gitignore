# Compiled binaries
fss_manager
fss_console
worker

# Object files
*.o

# Named pipes (created at runtime)
fss_in
fss_out

# Runtime logs
*_logfile.txt

# Test target directories (generated by fss_manager during sync)
tests/folder2/
tests/folder4/
tests/folder6/
SKIP_BECAUSE_OF_DUPLICATE/
